{
  "name": "liveserver",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
  "build": "node build.js",
  "watch": "node build.js --watch"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.25.12"
  }
}
